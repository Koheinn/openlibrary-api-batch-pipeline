# openlibrary-api-batch-pipeline
A Python batch data pipeline that extracts programming-related books from the Open Library API, handles pagination, retrieves author details and works, and stores the processed data as JSON
